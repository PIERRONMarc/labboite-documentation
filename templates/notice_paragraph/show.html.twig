{% extends 'base.html.twig' %}

{% block title %}NoticeParagraph{% endblock %}

{% block body %}
    <h1>NoticeParagraph</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ notice_paragraph.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ notice_paragraph.title }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ notice_paragraph.content }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('notice_paragraph_index') }}">back to list</a>

    <a href="{{ path('notice_paragraph_edit', {'id': notice_paragraph.id}) }}">edit</a>

    {{ include('notice_paragraph/_delete_form.html.twig') }}
{% endblock %}
