{% extends 'base.html.twig' %}

{% block title %}Category index{% endblock %}

{% block body %}
    <h1>Category index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>DisplayOrder</th>
                <th>ThumbnailName</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for category in categories %}
            <tr>
                <td>{{ category.id }}</td>
                <td>{{ category.name }}</td>
                <td>{{ category.displayOrder }}</td>
                <td>{{ category.thumbnailName }}</td>
                <td>
                    <a href="{{ path('category_edit', {'slug': category.slug}) }}">edit</a>
                    <a href="{{ path('tool_index', {'slug': category.slug}) }}">see tools</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('category_new', {'slug': theme.slug}) }}">Create new</a>
{% endblock %}
