{% extends 'base.html.twig' %}

{% block title %}Edit User{% endblock %}

{% block body %}
    <h1>Edit User</h1>

    {{ form_start(userForm) }}
        {{ form_row(userForm.username) }}
        {{ form_row(userForm.mail) }}
        {{ form_row(userForm.isSuperAdmin) }}

        <button class="btn">Update</button>
    {{ form_end(userForm) }}

    <a href="{{ path('user_index') }}">back to list</a>
{% endblock %}
